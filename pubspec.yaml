name: dashboardmevi
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  web_socket_channel: ^3.0.1
  http: ^1.1.0
  flutter_dotenv: ^5.1.0
  flutter_bloc: ^8.1.6
  bloc: ^8.1.4
  equatable: ^2.0.5

  # UI & Animation
  animations: ^2.0.11
  google_fonts: ^6.1.0
  # lucide_icons_flutter: ^1.1.0
  lucide_icons_flutter: ^3.1.15
  quickalert: ^1.1.0

  # Charts & Data Visualization
  fl_chart: ^1.0.0

  # Date & Time
  intl: ^0.19.0

  # Maps & Location - ARM64 compatible with minimal features
  flutter_map: ^6.1.0 # Stable version for ARM64
  latlong2: ^0.9.1 # Required for flutter_map_geojson
  google_maps_flutter: ^2.8.0 # ARM64 compatible
  flutter_map_geojson: ^1.0.8
  geolocator: ^10.1.0 # More stable for ARM64
  # geolocator_linux: ^0.2.1  # Disable for ARM64 issues
  # geoclue: ^0.1.1  # Disable for ARM64 issues
  geocoding: ^2.1.1 # ARM64 compatible
  location: ^5.0.3 # ARM64 compatible
  flutter_compass: ^0.8.0
  open_route_service: ^1.2.6
  nominatim_geocoding: ^0.0.6
  # flutter_map_tile_caching: ^9.1.4  # Disable for ARM64 memory issues

  # External Services
  url_launcher: ^6.3.0

  # Connectivity & Network
  connectivity_plus: ^6.0.5

  # Data Storage
  shared_preferences: ^2.2.2
  path_provider: ^2.1.5
  file_picker: ^8.1.2 # Update for Android embedding v2 support
  permission_handler: ^11.3.1 # For runtime storage permissions on Android
  device_info_plus: ^10.1.0 # For detecting Android SDK version

  # Dependency Injection
  get_it: ^8.0.0

  # Camera & Video (conditionally used) - ARM64 compatible versions
  video_player: ^2.8.6 # Downgrade for better ARM64 compatibility
  camera: ^0.10.6 # Downgrade for better ARM64 compatibility
  flutter_webrtc: ^0.12.3 # Update for Android embedding v2 support

  # 3D & Models (keep for MEVI 3D model)
  flutter_cube: ^0.1.1
  model_viewer_plus: any

  # Local HTTP server for the Windows 3D viewer: serves the app's own
  # bundled assets (assets/3d/car_model.glb, model_viewer_plus's JS) over
  # http://127.0.0.1 instead of file://, because Chromium's fetch() (used
  # internally by <model-viewer>) refuses to load file:// URLs. See
  # lib/services/local_asset_server_service.dart for the full explanation.
  shelf: ^1.4.1
  shelf_static: ^1.1.2

  # Platform-specific - ARM64 compatible
  window_manager: ^0.3.7 # Downgrade for ARM64 compatibility
  fvp: ^0.35.1
  # WebView2-backed 3D viewer for Windows only, see
  # lib/ui/core/widgets/sensors/car_3d_viewer_windows.dart. This lets
  # Windows render assets/3d/car_model.glb through the same Chromium
  # engine Chrome uses, instead of the flutter_cube/OBJ fallback in
  # car_3d_viewer_desktop.dart (still used on Linux/macOS, since this
  # package has no implementation for those platforms).
  webview_windows: ^0.4.0
  # webview_flutter_windows: ^1.1.1
  # webview_flutter_windows was removed: it doesn't back model_viewer_plus
  # on Windows (that package has no Windows implementation to plug it
  # into — see lib/ui/core/widgets/sensors/car_3d_viewer_desktop.dart for
  # the full explanation) and nothing else in the app used it.
  # webview_windows (above) is a separate, Windows-specific package that
  # car_3d_viewer_windows.dart drives directly instead.
dev_dependencies:
  flutter_test:
    sdk: flutter

  # Testing
  mockito: ^5.4.4
  build_runner: ^2.4.12
  bloc_test: ^9.1.7
  mocktail: ^1.0.4
  fake_async: ^1.3.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  very_good_analysis: ^10.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/3d/
    - assets/3d/car_model.glb
    - assets/internal_route.geojson
    - assets/mevi3d.mtl
    - assets/mevi3d.obj
    - assets/waypoints/
    - .env

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
